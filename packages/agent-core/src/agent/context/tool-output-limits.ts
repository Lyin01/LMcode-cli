/** Maximum tool-result size retained in model-visible conversation history. */
export const MAX_TOOL_RESULT_TOKENS = 16_000;

export const TOOL_TRUNCATION_NOTICE =
  '\n[content truncated — use read_file to re-read if needed]';
