Invoke a registered skill from the current skill listing. BLOCKING REQUIREMENT: when a skill from the listing genuinely matches the user's request, you MUST call this tool (not free-form text). A genuine match means the user explicitly named the skill or its specialized workflow is materially needed; broad keyword overlap, thematic similarity, or a generic request for complete output is not enough. Use the smallest set of skills that covers the task. Do NOT call the same skill repeatedly inside one turn — recursive depth is capped at {{ MAX_SKILL_QUERY_DEPTH }}.
