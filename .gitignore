node_modules/
dist/
dist-native/
.tmp-api-extractor/
.tmp-codex-desktop-smoke/
coverage/
*.tsbuildinfo
.vitest-results/
.DS_Store
.playwright-mcp/
.claude
.conductor
.lmcode
# …except test fixtures that deliberately ship a .lmcode dir: the unanchored
# `.lmcode` rule above swallowed them, so they existed only on the author's
# machine and every CI run failed on the missing fixture.
!packages/migration-legacy/test/fixtures/**/.lmcode
.lmcode-stash-dir
plugins/cdn/
superpowers
dream-lock.json
# Session export artifact accidentally written to the repo root by the
# export tests/CLI; regenerated (with a fresh timestamp) on every run.
session_default_output.zip
# fastembed model cache: its default cacheDir is the process cwd, so running
# the test suite (or the dev CLI) from the repo root downloads ~90 MB here.
local_cache/

# Runtime credentials and per-environment overrides
.env
.env.*
!.env.example
